# =============================================================================
# memo version
# =============================================================================
known = {0:0, 1:1}
def fib(n):
    if n in known:
        return known[n]
    addup = fib(n-1) + fib(n-2)
    known[n] = addup
    return addup

    